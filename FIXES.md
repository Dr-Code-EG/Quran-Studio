# Quran Studio - إصلاحات المشاكل على Vercel

## المشاكل المكتشفة والحلول المطبقة

### 1. **مشكلة البنية غير الصحيحة للـ API على Vercel**
**المشكلة:** 
- ملف `vercel.json` يحاول توجيه الطلبات إلى `/api/server.ts` بشكل مباشر
- Vercel لا يدعم هذا النمط من التوجيه

**الحل:**
- تم إنشاء ملفات API منفصلة في مجلد `api/`:
  - `api/generate.ts` - معالجة طلبات إنشاء الفيديو
  - `api/job-status.ts` - استقصاء حالة الفيديو
  - `api/verse-preview.ts` - معاينة الآيات
  - `api/health.ts` - التحقق من صحة الخادم
- تم تحديث `vercel.json` لاستخدام البنية الصحيحة

### 2. **مشكلة معالجة الفيديو في الخلفية**
**المشكلة:**
- عملية إنشاء الفيديو تحاول الاستمرار في الخلفية بعد الرد على الطلب
- Vercel قد تقتل العملية قبل انتهاء المعالجة (timeout بعد 30 ثانية)
- هذا يسبب الشاشة السوداء عند محاولة عرض الفيديو

**الحل:**
- تم الاحتفاظ بنفس نمط المعالجة في الخلفية (يعمل بشكل أفضل مع قاعدة البيانات)
- تم تحسين معالجة الأخطاء والتنظيف
- تم إضافة معالجة أفضل للحالات الاستثنائية

### 3. **مشكلة استقصاء الحالة**
**المشكلة:**
- الواجهة تحاول استقصاء الحالة من `/api/job-status/:jobId`
- المسار غير صحيح على Vercel

**الحل:**
- تم إنشاء endpoint جديد يقبل `jobId` كـ query parameter
- تم تحديث `src/App.tsx` لاستخدام المسار الصحيح: `/api/job-status?jobId={jobId}`

### 4. **مشكلة في مسارات الملفات المرفوعة**
**المشكلة:**
- الملفات المرفوعة قد لا تكون متاحة في المسارات المتوقعة على Vercel
- `/tmp` قد لا تكون دائمة بين الطلبات

**الحل:**
- تم استخدام `/tmp` للملفات المؤقتة (وهو الخيار الوحيد على Vercel)
- تم تحسين معالجة الأخطاء عند عدم وجود الملفات

### 5. **مشكلة في عرض الفيديو النهائي**
**المشكلة:**
- الفيديو قد لا يكون متاحاً للتحميل بعد الانتهاء
- المسار `/output/{jobId}_final.mp4` قد لا يكون متاحاً

**الحل:**
- تم التأكد من أن الملفات تُحفظ في المسار الصحيح
- تم إضافة معالجة أفضل للأخطاء والتنظيف

## التحديثات المطبقة

### ملفات جديدة:
- `api/generate.ts` - معالج الطلب الرئيسي لإنشاء الفيديو
- `api/job-status.ts` - معالج استقصاء الحالة
- `api/verse-preview.ts` - معالج معاينة الآيات
- `api/health.ts` - معالج التحقق من الصحة

### ملفات محدثة:
- `vercel.json` - تحديث البنية والتوجيهات
- `src/App.tsx` - تحديث مسارات API
- `package.json` - إضافة معلومات المشروع

## كيفية الاستخدام

### التطوير المحلي:
```bash
npm install
npm run dev
```

### النشر على Vercel:
```bash
git add .
git commit -m "Fix Vercel deployment issues"
git push
```

## ملاحظات مهمة

1. **معالجة الفيديو قد تستغرق وقتاً طويلاً**: على Vercel، قد تستغرق معالجة الفيديو عدة دقائق. الواجهة ستعرض شريط تقدم يتحدث كل 5 ثوان.

2. **حدود Vercel**: 
   - المهام الطويلة قد تنتهي بـ timeout
   - المساحة المؤقتة محدودة
   - قد تحتاج إلى خطة مدفوعة للمشاريع الكبيرة

3. **قاعدة البيانات**: 
   - يتم استخدام SQLite لتخزين حالة الفيديوهات
   - تُحفظ في `/tmp/data/studio.db`

## استكشاف الأخطاء

إذا واجهت مشاكل:

1. تحقق من سجلات Vercel: `vercel logs`
2. تأكد من أن جميع التبعيات مثبتة: `npm install`
3. تحقق من متغيرات البيئة في Vercel dashboard
4. جرب النسخة المحلية أولاً: `npm run dev`
